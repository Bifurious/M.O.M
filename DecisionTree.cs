using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DecisionTree : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string beginningDecision;
        string creep;
        string shoot;
        string move;
        string attack;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
